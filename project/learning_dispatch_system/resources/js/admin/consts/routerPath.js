import { prefix } from '../consts/common.js'

export const common = prefix;
export const login = prefix + '/login';
export const top = prefix + '/top';
export const loginForget = prefix + '/login/forget';
export const passwordReset = prefix + '/password/reset/:token';